{% extends 'base.html.twig' %}

{% block body %}
<div class="container mt-4">
<h2>Editer la propriete</h2>

{{ form_start(form) }}
    {{ form_rest(form) }}
<div class="col-md-2"> {{ form_row(form.imageFile) }}</div>
<button class="btn btn-primary"> Editer</button>
{{ form_end(form) }}


</div>


{% endblock  %}
